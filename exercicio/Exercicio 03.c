//Faça um programa que o usuário digite um numero, o programa o receba e faça a tabuada dele multiplicando pelos números de 0 a 9. Mostre a multiplicação sendo realizada e os resultados.
#include<stdio.h>
main()
{
	float numero, calculo;
	 printf("Insira o numero a ser calculado: \n");
	 scanf("%f",&numero);
	 
	calculo=(numero*0, numero*1, numero*2, numero*3);
	
	printf("resultado:%2.f",calculo);
	
	return(0);
}
